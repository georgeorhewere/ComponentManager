import { ScheduleMeetingDays } from './schedulemeetingdays';
import {expect, describe} from "@angular/core/testing/src/testing_internal";

describe('Schedulemeetingdays', () => {
  it('should create an instance', () => {
    expect(new ScheduleMeetingDays()).toBeTruthy();
  });
});
